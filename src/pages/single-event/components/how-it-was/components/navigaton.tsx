import React from 'react';
import { Box } from '@mui/material';

const Navigation: React.FC = () => {
  return <Box component="section" display="flex" flexDirection="column"></Box>;
};

export default Navigation;
